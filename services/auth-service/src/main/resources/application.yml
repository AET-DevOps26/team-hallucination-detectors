server:
  port: 8080

spring:
  application:
    name: api-service
  datasource:
    url: jdbc:postgresql://database:5432/vibeshield
    username: vibeshield
    password: vibeshield

  jpa:
    hibernate:
      ddl-auto: update

vibeshield:
  cors:
    allowed-origins: ${VIBESHIELD_CORS_ALLOWED_ORIGINS:http://localhost:3000}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
