apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: auth-service
          image: "{{ .Values.images.authService }}:{{ .Values.images.tag }}"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          env:
            - name: APP_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: vibeshield-secrets
                  key: APP_JWT_SECRET
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: vibeshield-secrets
                  key: SPRING_DATASOURCE_PASSWORD
            - name: SPRING_MAIL_HOST
              value: {{ .Values.authService.mail.host | quote }}
            - name: SPRING_MAIL_PORT
              value: {{ .Values.authService.mail.port | quote }}
            - name: SPRING_MAIL_USERNAME
              value: {{ .Values.authService.mail.username | quote }}
            - name: SPRING_MAIL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: vibeshield-secrets
                  key: SPRING_MAIL_PASSWORD
            - name: SPRING_MAIL_SMTP_AUTH
              value: {{ .Values.authService.mail.smtpAuth | quote }}
            - name: SPRING_MAIL_SMTP_STARTTLS_ENABLE
              value: {{ .Values.authService.mail.starttls | quote }}
            - name: PASSWORD_RESET_MAIL_FROM
              value: {{ .Values.authService.mail.from | quote }}
            - name: PASSWORD_RESET_PAGE_URL
              value: {{ .Values.authService.passwordResetPageUrl | quote }}
            - name: OTEL_SERVICE_NAME
              value: "auth-service"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://tempo:4318"
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: "http/protobuf"
            - name: OTEL_TRACES_EXPORTER
              value: "otlp"
            - name: OTEL_METRICS_EXPORTER
              value: "none"
            - name: OTEL_LOGS_EXPORTER
              value: "none"
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 45
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
